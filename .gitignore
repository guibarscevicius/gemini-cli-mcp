node_modules/
dist/
*.js.map
*.d.ts.map
.env
.env.*
*.pem
*.key
npm-debug.log*
.DS_Store

# Claude Code project-local state (settings, subagent memory — not team-shared)
.claude/

# Scratch/temp files — put one-off experiments here, never in the repo root
scratch/

# Internal development notes (test plans, session logs, personal paths)
tasks/
